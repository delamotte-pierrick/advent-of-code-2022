use crate::utils::read_lines;
use array_tool::vec::Intersect;

pub(crate) fn day_X_1() {
    if let Ok(lines) = read_lines("./data/input_X.txt") {
        for line in lines {
            if let Ok(content) = line {}
        }
    }
    println!("Result: {}", 0);
}

pub(crate) fn day_X_2() {
    println!("Result 2: {}", 0);
}
